export const HERO_WORDS = [
  'beautiful',
  'engaging',
  'intuitive',
  'polished',
  'dynamic',
  'stunning',
] as const;

export const ANIMATION_DEFAULTS = {
  INTERVAL_DURATION: 1800,
  TRANSITION_DURATION: 500,
  MIN_WIDTH: 120,
  CHAR_WIDTH_ESTIMATE: 8,
} as const;

export const CSS_CLASSES = {
  BASE: 'text-text-green transition-all duration-500 inline-block',
} as const;
